steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - function_cicd
  - --region=global
  - --source=.
  - --trigger-http
  - --runtime=python310
  - --allow-unauthenticated
options:
  logging : CLOUD_LOGGING_ONLY
